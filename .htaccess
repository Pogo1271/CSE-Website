RewriteEngine On

# Allow existing files/folders through (assets, images, contact.php, etc.)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Everything else goes to the SPA entry
RewriteRule ^ index.html [L]